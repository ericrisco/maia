[project]
name = "maia"
version = "0.0.0"
description = "MAIA — Model Andorrà d'Intel·ligència Artificial: an open Gemma 4 12B QLoRA fine-tune + RAG for Andorran Catalan."
readme = "README.md"
requires-python = "==3.11.*"
license = { text = "Apache-2.0" }
authors = [{ name = "Eric Risco de la Torre" }]
keywords = ["andorra", "catalan", "llm", "gemma", "qlora", "rag"]
dependencies = [
    "anthropic>=0.120.0",
    "fastapi>=0.115",
    "huggingface-hub>=1.24.0",
    "mwparserfromhell>=0.6",
    "pyarrow>=25.0.0",
    "pydantic>=2.9",
    "pypdf>=6.14.2",
    "pyyaml>=6.0.3",
    "requests>=2.31",
    "trafilatura>=1.8",
    "types-pyyaml>=6.0.12.20260724",
]

[project.optional-dependencies]
# Local speech-to-text on Apple silicon (M1.13). Optional because `mlx-whisper` exists only on
# Apple silicon and CI is Linux; `imageio-ffmpeg` ships a static ffmpeg so no system install is
# needed. Install with `uv sync --extra audio`.
audio = [
    "mlx-whisper>=0.4",
    "imageio-ffmpeg>=0.5",
]

[project.urls]
Homepage = "https://github.com/ericrisco/maia"
Repository = "https://github.com/ericrisco/maia"

[project.scripts]
maia-validate-corpus = "maia.corpus.validate:main"
maia-consolidate-corpus = "maia.corpus.consolidate:main"
maia-publish-corpus = "maia.corpus.publish:main"
maia-corpus-report = "maia.corpus.coverage:main"
maia-sample-corpus = "maia.corpus.sampling:main"
maia-validate-dataset = "maia.synth.validate:main"
maia-taxonomy = "maia.synth.taxonomy:main"
maia-glossary = "maia.synth.glossary:main"
maia-generate = "maia.synth.generate:main"
maia-mix-general-ca = "maia.synth.general_ca:main"
maia-filter-dataset = "maia.synth.filter:main"
maia-pilot = "maia.synth.pilot:main"
maia-colab-notebook = "maia.training.colab:main"
maia-generate-full = "maia.synth.run:main"
maia-split = "maia.synth.splits:main"
maia-distribution = "maia.synth.distribution:main"
maia-publish-dataset = "maia.synth.publish:main"
maia-transcribe = "maia.scraping.transcribe:main"
maia-rtva-editorial = "maia.scraping.rtva:main"
maia-consell-diari = "maia.scraping.consell:main"
maia-latency-gate = "maia.serving.latency:main"
maia-e2e = "maia.serving.e2e:main"
maia-endpoint-config = "maia.serving.serverless:main"
maia-demo = "maia.serving.demo:main"

[dependency-groups]
dev = [
    "httpx>=0.28",
    "ruff>=0.9",
    "mypy>=1.14",
    "pytest>=8.3",
    "pytest-cov>=6.0",
    "pre-commit>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/maia"]

# ─────────────────────────────────────────────────────────────
# Ruff — lint + format
# ─────────────────────────────────────────────────────────────
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
]

# The typographic apostrophe is Catalan orthography (`d’acord`), not an accidental homoglyph:
# this project parses it, repairs it and stores it, so RUF001-3 must not flag it as a lookalike
# for the ASCII quote. Everything else stays flagged.
allowed-confusables = ["’"]

[tool.ruff.lint.pydocstyle]
convention = "google"

# ─────────────────────────────────────────────────────────────
# mypy — strict
# ─────────────────────────────────────────────────────────────
[tool.mypy]
python_version = "3.11"
strict = true
warn_unreachable = true
warn_redundant_casts = true
warn_unused_ignores = true
disallow_untyped_defs = true
files = ["src", "tests"]

# pyarrow, transformers and unsloth ship no py.typed marker, and the last two are only ever
# imported inside a function behind a blocked-by-resource seam. Scoped so the rest stays strict.
[[tool.mypy.overrides]]
module = ["pyarrow.*", "transformers.*", "unsloth.*", "trl.*", "gradio.*", "mlx_whisper.*", "imageio_ffmpeg.*"]
ignore_missing_imports = true

# ─────────────────────────────────────────────────────────────
# pytest — layered testing (unit fast; integration behind marker)
# ─────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-ra --strict-markers"
markers = [
    "unit: fast tests with no external services (default fast gate)",
    "integration: tests against real dependencies (ephemeral containers/services)",
]

# ─────────────────────────────────────────────────────────────
# Coverage — gate over the FULL suite (unit + integration)
# ─────────────────────────────────────────────────────────────
[tool.coverage.run]
branch = true
source = ["src/maia"]

[tool.coverage.report]
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
